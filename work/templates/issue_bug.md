<!-- Paste-ready skeleton for one filed issue; the rules behind it are ../issue_style.md.
     Title: `component: symptom`. Not a sentence, and not the fix. -->

**Problem**
<!-- What is broken and what it costs. 3 lines max. No history, no diagnosis. -->

**Solution**
<!-- What to do, 2 lines max. If you do not own the answer, write who does:
     `Unknown: needs a ruling from <name> on <the question>`. Naming the decider IS the
     actionable part; a guess costs them an argument before any work starts. -->

**Reproduce**
```
<!-- one command, and what you see -->
```

**Environment**
<!-- Toolchain version, branch, OS: what stops a "works for me" reply. -->

## Tasks
<!-- Delete this section if it is a single step. Items owned by different people are two issues
     with a dependency link, not one. -->
- [ ]

**Done when:**
<!-- The observable that settles it. Without this the issue gets closed twice. -->

<details><summary>Details</summary>
<!-- Evidence, `path:line`, commit ids, measurements, what you ruled out. Link rather than paste:
     a link survives a refactor. Paste only the error text itself. -->
</details>

<!-- Labels: exactly one from KIND_LABELS in project.conf, none at all when that key is empty,
     none a workflow derives when LABELS_DERIVED is yes, and never an invented name, which most
     trackers create silently on first use. Leave triage to whoever picks this up. -->
